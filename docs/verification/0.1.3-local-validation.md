# 0.1.3 local validation

The [2026-09-22 release-candidate follow-up](../../reports/2026-09-22-0.1.3-release-validation.md)
adds fresh package E2E 256/256, one exact artifact with license/file/secret checks,
consumer import checks, and bounded native Safari 26.6.2 interactions across all
four Playground adapters. It does not claim full native Safari or device coverage.

최신 패키지 파일·소비자 검증과 실제 Safari의 제한된 직접 조작 결과는 위 후속 기록에
있습니다. 아래 9월 14일 잔여 항목은 당시 검증 범위이며, 현재 범위는 후속 기록을 따릅니다.

The [2026-09-22 documentation alignment follow-up](../../reports/2026-09-22-playground-documentation.md)
records adapter-specific descriptions/APIs, reusable source files, and live locale
updates. It includes fresh verify, Playground 327/327, performance 1/1, and the
final source-panel checks. Earlier evidence below keeps its original scope.

2026-09-22 설명·API·소스 보기·언어 전환 정정과 최신 검증은 위 후속 기록에서
확인합니다. 아래 과거 결과와 GIF는 각 촬영·검증 당시 범위를 유지합니다.

The [2026-09-16 Playground guidance follow-up](../../reports/2026-09-16-nested-example-guidance.md)
records the later example-title and instruction changes, with fresh production
Playground and performance checks. The reset/feedback evidence below retains
its original 2026-09-14 scope.

이후 예제명·설명 변경과 최신 Playground·성능 검증은 위 후속 기록에서 확인합니다.
아래 초기화·미리보기 검증은 2026-09-14 당시 범위를 유지합니다.

The [refreshed GIF capture](../playground-preview.md) on 2026-09-16 uses the
working source with those fixes and guidance changes. Its four React scenes
are direct interaction evidence, separate from the automated gates below.

2026-09-16의 새 GIF는 최신 수정과 예제 안내를 포함한 작업 소스로 촬영했습니다.
React 장면 네 개의 직접 조작 기록이며 아래 전체 자동 검증과는 범위가 다릅니다.

Recorded on 2026-09-14 KST for the local changes on top of
`044d35f71178604d6daf0e1f8c6e3710a66a6513`, branch `codex/0.1.3-stability`.
The package manifest is 0.1.3. These results cover the working source after the
Playground reset and insertion-feedback fixes, not a published package.

2026-09-14 KST 기준, `044d35f` 이후 로컬 초기화·미리보기 수정의 검증 기록입니다.
패키지 manifest는 0.1.3이며 배포 완료나 최신 tarball 검증을 의미하지 않습니다.

## Behavior and scenarios

| Scenario | Checked result |
| --- | --- |
| Select Build, press Reset, then Shift-click Review | All four adapters clear selected IDs, styling, and the old range anchor. Only Review is selected and moved on the next drag. |
| Select body text after moving Review | The text remains selectable. The latest direct check selected part of the label; it did not test clipboard paste. |
| Drag to the scroll container's bottom padding outside the registered drop area | Insertion feedback hides while the move placeholder retains its space and scroll range. Releasing outside cancels without changing the order. |
| Reenter a valid destination | Feedback returns and releasing commits the intended order. Escape cancels and removes temporary feedback. |
| Use custom or skeleton placeholders | Outside hiding, reentry, drop, and cancel preserve the same behavior. |

일반 데이터 갱신의 선택 유지 동작은 그대로이며 Playground의 명시적 초기화에서만
등록을 다시 생성합니다. 드롭 판정 영역과 자동 스크롤 속도 계산은 이번 두 수정에서
변경하지 않았습니다. 사용법은 [고급 정렬](../ko/16-advanced-sorting.md),
[자동 스크롤](../ko/10-animation-auto-scroll.md),
[Placeholder](../ko/13-placeholder.md)를 참고합니다.

## Automated validation

These gates ran after the runtime and regression-test changes. The subsequent
documentation update did not rerun the browser or performance suites.

| Command | Result |
| --- | --- |
| `npm run verify` | PASS: policy 51/51, unit 272/272, licenses, typecheck, build, public types |
| `npm run verify:e2e -- --workers=3` | PASS: 256/256, including Chromium, Firefox, WebKit, and resource checks |
| `CI=1 npm run verify:playground -- -- --workers=3` | PASS: production build and 303/303 across Chromium, Firefox, and WebKit |
| `CI=1 npm run verify:performance` | PASS: 1/1, Chromium |

The performance sample was 4.74 → 4.93 MB JS heap, with DOM nodes 492 → 492,
event listeners 378 → 378, and live nodes 153 → 153. This is a bounded repeat
test, not evidence of long-duration leak freedom or physical high-refresh
display behavior.

위 수치는 구현 수정 후 실행한 전체 게이트 결과입니다. 이후 문서 수정만으로
브라우저·성능 게이트를 다시 실행한 것은 아닙니다. 성능 표본은 제한된 반복 테스트의
통과 근거이며 장시간 누수 부재나 실물 고주사율 장치 검증을 뜻하지 않습니다.

After the documentation update, `npm run test:policy` passed 51/51, including
the guide pairs, 18 complete TypeScript examples, public API coverage, and
user-documentation links.

문서 갱신 후 `npm run test:policy` 51/51을 다시 통과했습니다. 한·영 가이드 쌍,
전체 TypeScript 예제 18개, 공개 API 설명, 사용자 문서 링크를 포함합니다.

## Direct interaction and remaining coverage

The production Playground was operated with Playwright CLI in headed Chromium
at 1440 × 1100 on 2026-09-14, 10:41–10:46 KST. Actual handle clicks, modifier
keys, pointer movement, and release verified reset/Shift selection in React,
Vue, and Svelte; a subsequent React move and body-text selection; Svelte edge
scrolling and reentry; and React outside-drop cancellation. Ten screenshots
and the trace remain local evidence. The
[implementation report](../../reports/2026-09-14-playground-reset-feedback.md)
records their locations, the initial failures, and final results.

- Native Safari has not been rerun for these latest fixes. Playwright WebKit
  results do not establish native Safari coverage.
- The [earlier preparation record](../../reports/2026-09-14-0.1.3-preparation.md)
  contains native Safari checks and a tarball/consumer check from before these
  fixes. Regenerate and verify the exact artifact before releasing them.
- Physical touch/pen devices, physical high-refresh displays, and clipboard
  paste after these latest fixes were not part of this direct check.
- This record covers local implementation and validation; it does not record a
  remote push, merge, tag, or publication.

실제 마우스·보조키 조작으로 초기화 후 선택, 이동 후 본문 선택, 스크롤 경계의
미리보기 숨김·재진입·영역 밖 취소를 확인했습니다. 스크린샷 10개와 trace는 로컬
증거로 보존했습니다. 최신 수정의 실제 Safari, 실물 touch/pen·고주사율 장치,
클립보드 붙여넣기는 미검증입니다. 이전 tarball·소비자 검증 결과를 최신 수정에
적용할 수 없으며 릴리스 전에 같은 최종 아티팩트로 재검증해야 합니다.
