# 2026-08-12 Comins Sortable Task 4 및 Task 5

## 작업 일시

- 2026-08-12 (Asia/Seoul)

## 요약

- 승인된 Comins Sortable v1 구현 계획의 Task 4를 구현했다.
- cleanup callback을 LIFO 순서로 한 번만 실행하고 category별 활성 개수를
  추적하는 내부 `ResourceBag`을 추가했다.
- mount 이후 element의 `ownerDocument`와 `defaultView`에서만 browser service를
  만드는 `SortablePlatform`을 추가했다.
- `idle`, `pending`, `dragging`, `committing`, `cancelling` 전이만 허용하는
  discriminated session state machine을 추가했다.
- primary Pointer Events, 4 CSS px Euclidean activation, handle/ignore 우선순위,
  rAF당 한 번의 최신 이동, pointer capture와 종료 cleanup을 구현했다.
- root 또는 `./core` 공개 export, runtime dependency, package boundary 및 원격
  상태는 변경하지 않았다.

## 변경 파일

- `src/core/resources.ts`
- `src/core/platform.ts`
- `src/core/session.ts`
- `src/core/pointer.ts`
- `test/unit/helpers/core-fixtures.ts`
- `test/unit/core/resources.test.ts`
- `test/unit/core/platform.test.ts`
- `test/unit/core/session.test.ts`
- `test/unit/core/pointer.test.ts`
- `reports/2026-08-12.md`

## TDD 증거

- 초기 RED:
  `node --import tsx --test test/unit/core/resources.test.ts test/unit/core/session.test.ts test/unit/core/pointer.test.ts`
  - 구현 module이 없어 3개 test file 모두 `ERR_MODULE_NOT_FOUND`로 실패했다.
- browser platform 및 cleanup-order 보정 RED:
  - `createBrowserPlatform` export 부재와 active callback 오류 전 listener가
    정리되지 않는 두 계약 실패를 확인했다.
- pointer capture 보정 RED:
  - capture 설정·해제 예외가 activation/release lifecycle 밖으로 전파되는
    두 실패를 확인했다.
- 최종 focused GREEN:
  - resource, platform, session, pointer test 20/20 통과.

## 검증

- `npm run typecheck`: 통과.
- `npm run verify`: 통과.
  - policy test 26/26 통과.
  - 전체 Core unit test 49/49 통과.
  - license, TypeScript, ES2020 ESM build, public type fixture 통과.

## 잔여 리스크

- Task 4는 pointer activation foundation까지만 제공한다. Registry, geometry,
  collision, feedback 및 transaction을 하나의 Scope lifecycle로 연결하는 작업은
  Task 5 범위다.
- Placeholder, auto-scroll, DOM commit/rollback, Vanilla 및 framework adapter는
  아직 구현되지 않았다.
- Browser E2E는 실제 DOM fixture가 추가되는 Task 11에서 적용한다.
- 원격 push, PR, merge, publish, tag 및 Release는 수행하지 않았다.

---

## Task 5 작업 일시

- 2026-08-12 (Asia/Seoul)

## Task 5 요약

- 공개 `createSortableScope()`와 area 등록·갱신·해제 계약을 추가했다.
- same-tag empty Placeholder, source feedback, focus·inline style 복원 및 영역 간 이동과
  동일 영역 재정렬 rollback을 구현했다.
- 가장 가까운 scroll container 우선의 auto-scroll과 pointer가 정지한 동안의 연속
  rAF scrolling, 종료 시 frame cleanup을 구현했다.
- Task 3 geometry cache를 Scope에 연결해 activation·area 진입·scroll·Placeholder
  이동·framework update 시점에만 필요한 rect를 갱신하도록 했다.
- `before → start → drag → insert → change → after` 순서, 다음 rAF commit 확인,
  `state-not-committed` rollback 및 고정 status mapping을 구현했다.
- activation 전 ID 오류, active callback 오류, `onAfterDrag` 오류, unmount·destroy 및
  commit 확인 대기 중 취소를 각각 승인된 cleanup 순서로 처리했다.
- 첫 public area 등록 실패 후 platform이 잘못 고정되지 않도록 초기화 transaction을
  보완하고, destination `accept`를 pointer frame당 한 번만 평가하도록 했다.

## Task 5 변경 파일

- `src/core.ts`
- `src/core/model.ts`
- `src/core/pointer.ts`
- `src/core/geometry.ts`
- `src/core/collision.ts`
- `src/core/feedback.ts`
- `src/core/auto-scroll.ts`
- `src/core/scope.ts`
- `test/types/core.test.ts`
- `test/unit/helpers/core-fixtures.ts`
- `test/unit/helpers/scope-fixtures.ts`
- `test/unit/core/pointer.test.ts`
- `test/unit/core/collision.test.ts`
- `test/unit/core/feedback.test.ts`
- `test/unit/core/auto-scroll.test.ts`
- `test/unit/core/scope.test.ts`
- `test/unit/core/event-order.test.ts`
- `test/unit/core/resource-lifecycle.test.ts`
- `reports/2026-08-12.md`

## Task 5 TDD 증거

- 초기 RED: feedback, auto-scroll, Scope module이 없어 6개 focused test file이
  `ERR_MODULE_NOT_FOUND`로 실패했다.
- pointer 통합 RED: activation 거부 boolean, owner cancel 및 sensor 종료 callback
  계약 부재를 확인했다.
- lifecycle 보정 RED: commit 확인 대기 중 destroy/unmount frame 잔존과 rollback 시
  원래 형제 순서 미복원 3건을 확인했다.
- 추가 경계 RED: 동일 영역 원래 index 복원, geometry cache 재사용, 연속
  auto-scroll, interaction `getItemId` 예외 격리, 첫 등록 실패 후 document 재결합,
  frame당 단일 `accept` 평가 실패를 각각 재현했다.
- 모든 RED를 최소 구현으로 GREEN 전환하고 관련 focused suite를 재실행했다.

## Task 5 검증

- `npm run verify`: 통과.
  - policy test 26/26 통과.
  - 전체 Core unit test 79/79 통과.
  - license, TypeScript typecheck, ES2020 ESM build, public type fixture 통과.
- built `dist/core.js`의 DOM global 없는 import와 `createSortableScope` export 확인:
  통과.
- `git diff --check`: 통과.

## Task 5 잔여 리스크

- Task 5는 Core Scope까지 완료했다. Vanilla root facade와 최소 CSS는 Task 6 범위다.
- React·Vue·Svelte adapter와 실제 browser E2E는 각각 후속 Task 7~11 범위다.
- 원격 push, PR, merge, publish, tag 및 Release는 수행하지 않았다.

---

## Task 6 작업 일시

- 2026-08-12 (Asia/Seoul)

## Task 6 요약

- package root의 `createSortable()` Vanilla facade와 `Sortable`/Vanilla option type을 추가했다.
- 첫 area와 추가 area의 selector를 호출 시점에만 해석하고, selector syntax 오류·미존재
  element를 raw selector 없이 `INVALID_ELEMENT`으로 정규화했다.
- Core `onChange` 직전에 source의 원본 parent/sibling snapshot을 저장하고 Placeholder 위치에
  source를 DOM으로 적용하는 transaction을 추가했다. callback 오류 및 non-drop 결과에서는
  original DOM을 복원하며, dropped 결과에서는 snapshot을 해제한 뒤 user `onAfterDrag`를 호출한다.
- 최소 namespace CSS를 `dist/styles.css`로 복사하고 root/core/CSS export 및 CSS side effect
  manifest contract를 갱신했다.

## Task 6 변경 파일

- `src/index.ts`
- `src/styles.css`
- `src/vanilla/types.ts`
- `src/vanilla/dom-transaction.ts`
- `src/vanilla/create-sortable.ts`
- `tsconfig.build.json`
- `package.json`
- `test/package-foundation.node.mjs`
- `test/unit/helpers/core-fixtures.ts`
- `test/unit/vanilla/dom-transaction.test.ts`
- `test/unit/vanilla/create-sortable.test.ts`
- `test/types/root.test.ts`
- `reports/2026-08-12.md`

## Task 6 TDD 증거

- 초기 RED:
  `node --import tsx --test test/unit/vanilla/dom-transaction.test.ts test/unit/vanilla/create-sortable.test.ts`
  - `src/index.js` 및 `src/vanilla/dom-transaction.js` 부재로 `ERR_MODULE_NOT_FOUND` 2건을
    확인했다.
- fixture 보정 RED:
  - public scope가 `Window.requestAnimationFrame`을 사용하면서 fake Window의 해당 계약이
    없어 integration test 2건이 실패한 것을 확인하고, fixture를 실제 frame queue에 연결했다.
- policy RED:
  - 최초 `npm run verify`에서 기존 package policy가 `sideEffects: false`를 요구해 1건
    실패한 것을 확인했다. Task 6의 승인된 root/CSS export contract에 맞춰 policy expectation을
    갱신했다.
- 리뷰 보강:
  - next-frame DOM order 불일치의 rollback-before-`onAfterDrag` 및 `updateArea()` 이후
    custom `getItemId`/item selector transaction 동기화 test를 추가했다.
- GREEN:
  - Vanilla focused test 7/7, 전체 unit test 86/86, package policy 26/26이 통과했다.

## Task 6 검증

- `node --import tsx --test test/unit/vanilla/dom-transaction.test.ts test/unit/vanilla/create-sortable.test.ts`: 7/7 통과.
- `npm run verify`: 통과.
  - license check, policy 26/26, TypeScript typecheck, unit 86/86, build, public type fixture 통과.
- `node --input-type=module --eval "await import('./dist/index.js'); await import('./dist/core.js')"`:
  DOM global을 설정하지 않은 상태에서 통과.
- `dist/styles.css`: source와 byte-equivalent이며 namespace selector와 reduced-motion rule만 존재함을 확인.
- `git diff --check`: 통과.

## Task 6 잔여 리스크

- Vanilla facade의 실제 browser E2E와 React/Vue/Svelte adapter는 후속 Task 범위다.
- 원격 push, PR, merge, publish, tag 및 Release는 수행하지 않았다.

---

## Task 6 Fix Round 1

- broad `item: "li"` selector가 internal placeholder를 consumer item으로 읽어
  `MISSING_ITEM_ID`와 rollback을 일으키던 문제를 Core registry/scope와 Vanilla
  transaction의 direct-item 집계에서 placeholder를 제외해 수정했다.
- structural non-Element 입력이 raw `TypeError` 대신 `INVALID_ELEMENT`으로
  정규화되도록 cross-realm-safe Element guard를 강화했다.
- TDD RED: focused Vanilla test에서 broad selector rollback 및 raw TypeError를 재현했다.
- GREEN: focused Vanilla 9/9, `npm run verify` policy 26/26·unit 88/88, built import,
  CSS namespace 및 diff check를 통과했다.
- 원격 push, PR, merge, publish, tag 및 Release는 수행하지 않았다.

---

## Task 7 작업 일시

- 2026-08-12 (Asia/Seoul)

## Task 7 요약

- React·Vue·Svelte adapter가 공유할 private framework binding registry와 controlled transaction을 추가했다.
- `itemKey` property/callback에서 안정적 item ID를 해석하며, 누락 ID를 index로 대체하지 않고 `MISSING_ITEM_ID`로 거부한다.
- registry는 area ID와 동일 group의 item ID를 등록 시 검증하고, unregister 시 binding 및 그 callback 참조를 제거한다.
- transaction은 `change.orders` 순서로 affected setter를 정확히 한 번 호출한 후 root `onChange`를 한 번 호출한다. setter 또는 root callback의 partial failure에는 모든 원래 array를 복원한다.
- 명확화된 C 계약을 적용했다. `confirm`의 ID order는 최신 framework state의 `getItems()`와 `getItemId()`로만 비교하고, DOM은 placeholder 제외 direct child 수가 item 수와 같은지 확인하는 structural guard로만 사용한다. `getElement() === null`은 false다.
- public package export, framework import, runtime dependency는 추가하지 않았다.

## Task 7 변경 파일

- `src/framework/item-key.ts`
- `src/framework/bindings.ts`
- `src/framework/controlled-transaction.ts`
- `test/unit/helpers/framework-fixtures.ts`
- `test/unit/framework/item-key.test.ts`
- `test/unit/framework/bindings.test.ts`
- `test/unit/framework/controlled-transaction.test.ts`
- `reports/2026-08-12.md`

## Task 7 TDD 증거

- 초기 RED:
  `node --import tsx --test test/unit/framework/item-key.test.ts test/unit/framework/bindings.test.ts test/unit/framework/controlled-transaction.test.ts`
  - `src/framework/item-key.js` 및 `src/framework/bindings.js` 부재로 3개 test file 모두 `ERR_MODULE_NOT_FOUND`로 실패했다.
- 초기 test가 포착하는 production break:
  - missing item ID를 index로 fallback하는 오류,
  - duplicate area/group item ID와 unregister 뒤 stale callback 보존,
  - transfer/reorder setter 순서·횟수 또는 root enhanced change 누락,
  - setter/root callback partial failure 뒤 원 배열 미복원,
  - finish/destroy 뒤 stale snapshot rollback,
  - framework ID order, `getElement() === null`, placeholder 제외 direct-child structural guard 누락.
- 자체 리뷰 RED:
  - one-area 내부 duplicate item ID 허용을 재현했고, `DUPLICATE_ITEM_ID` 검증을 추가했다.
- 최종 focused GREEN:
  - framework unit test 11/11 통과.

## Task 7 검증

- `node --import tsx --test test/unit/framework/item-key.test.ts test/unit/framework/bindings.test.ts test/unit/framework/controlled-transaction.test.ts`: 11/11 통과.
- `npm run typecheck`: 통과.
- `npm run verify`: 통과.
  - license check, policy 26/26, typecheck, 전체 unit 99/99, build, public type fixture 통과.
- `.githooks/pre-commit`: 통과.

## Task 7 잔여 리스크

- `onAfterDrag`의 dropped/취소 결과에서 `finish` 또는 `rollback`을 호출하는 결합은 후속 framework adapter 범위다. Task 7은 이를 위한 internal transaction API와 cleanup semantics만 제공한다.
- C 계약상 DOM은 item ID source가 아니다. 동일 수의 잘못된 direct child identity는 adapter의 “각 render 결과가 정확히 하나의 direct child” 계약으로 보호하며, Task 7 `confirm`은 framework state ID order와 structural count만 검증한다.
- 원격 push, PR, merge, publish, tag 및 Release는 수행하지 않았다.

---

## Task 7 Review Fix Round 1

- registry registration과 transaction snapshot 경계에서 affected group의 모든 current binding을 한 번씩 scan하여, 등록 뒤 발생한 same-area 및 cross-area duplicate ID를 `DUPLICATE_ITEM_ID`로 거부하도록 보강했다.
- group validation이 arrays를 모두 한 번 읽은 뒤 수행되므로 apply의 affected binding `getItems()`는 validation과 snapshot에 걸쳐 정확히 한 번만 호출된다. validation failure에는 setter, root callback, pending original snapshot retention이 없다.
- apply setter와 rollback은 registry 재조회 대신 initial snapshot의 captured binding을 사용한다. 첫 setter가 다른 area를 unregister/re-register해도 replacement binding은 update 또는 rollback으로 오염되지 않는다.
- disposer는 area ID와 identity token만 capture하며 binding callback/element/adapter state 참조를 closure에 보관하지 않는다.
- rollback setter failure가 있어도 remaining original restore를 계속하고 최초 apply/root callback 오류를 유지하는 기존 동작을 committed regression으로 고정했다.
- Task 7 brief의 confirm 문구를 확정된 C 계약(state `getItems`/`getItemId` ID order + placeholder 제외 direct-child count structural guard, null false)으로 최소 수정했다.
- TDD: 신규 4건 RED(기존 binding internal/cross duplicate 재등록, apply snapshot validation, replacement binding update)를 재현했고, rollback-failure regression은 기존 구현이 이미 GREEN인 것을 확인했다. GREEN: framework 16/16, typecheck 통과. `npm run verify`는 policy 26/26·전체 unit 104/104·build·type fixture까지 통과했고 pre-commit도 통과했다.

---

## Task 8 작업 일시

- 2026-08-12 (Asia/Seoul)

## Task 8 요약

- `comins-sortable/react`에 `SortableRoot<T>`와 controlled `SortableArea<T>`만 공개했다.
- Root는 DOM wrapper 없이 context provider만 렌더하고, Area는 `data-comins-sortable-area`를 가진 하나의 `div`와 direct child render 결과만 렌더한다.
- Root 없는 Area는 private one-area Scope를 사용한다. Root 하위 Area는 Task 7 `BindingRegistry`/`ControlledTransaction`으로 transfer setter 두 번과 Root change 한 번을 같은 transaction에서 처리한다.
- Strict Mode setup-cleanup-setup에서 registration이 중복되지 않도록 disposer를 멱등 처리했고, component effect cleanup에서 controller를 destroy하지 않아 development Strict Mode의 재setup이 destroyed controller를 재사용하지 않는다.
- placeholder를 제외한 direct child 수와 controlled item 수가 다르면 activation ID 해석이 `INVALID_ELEMENT`으로 거부한다.
- `./react` ESM/types export만 추가했으며 dependency, version, 다른 public export는 변경하지 않았다.

## Task 8 변경 파일

- `src/react.ts`, `src/react/context.ts`, `src/react/SortableRoot.tsx`, `src/react/SortableArea.tsx`
- `test/unit/helpers/framework-fixtures.ts`, `test/unit/react/adapter.test.ts`, `test/types/react.test.tsx`
- `package.json`, `test/package-foundation.node.mjs`, `tsconfig.json`, `test/types/tsconfig.json`
- `reports/2026-08-12.md`

## Task 8 TDD 및 검증

- RED: React focused test는 `src/react/context.js` 부재로 `ERR_MODULE_NOT_FOUND`, type gate는 `comins-sortable/react` 부재로 `TS2307`을 확인했다. 기존 type test config가 `.tsx` fixture를 제외하고 있어 fixture 포함 후 type RED를 재실행했다.
- React focused GREEN: Strict Mode registration cleanup, rootless one-area Scope, transfer batching, rollback-before-`onAfterDrag`, option update non-reregistration, direct-child mismatch를 포함한 6/6 통과.
- `npm run typecheck`, `npm run build`, `npm run test:types`, DOM global 없는 `dist/react.js` import 통과.
- `npm run verify`: policy 26/26, 전체 unit 110/110, license/typecheck/build/public type fixture 통과.
- `.githooks/pre-commit`, `git diff --check`: 통과.

## Task 8 잔여 리스크

- injected sanitized harness로 lifecycle/transaction 계약을 검증했다. 실제 React renderer Strict Mode 및 pointer interaction browser E2E는 후속 browser task 범위다.
- Area unregister가 active session rollback과 listener cleanup을 수행한다. unmount 후 private/root controller는 외부 reference가 없어 수거되므로 public destroy API를 추가하지 않았다.
- 원격 push, PR, merge, publish, tag 및 Release는 수행하지 않았다.

---

## Task 8 Review Fix Round 1

- Core activation 전에 React binding의 placeholder 제외 direct-child count와 controlled item count를 검증하도록 `BindingRegistry.validateElements()`를 추가했다. 따라서 destination DOM이 비어 있지만 controlled items가 남은 상태는 user `onBeforeDragStart` 전에 `INVALID_ELEMENT`으로 거부된다.
- cancelled/rejected `handleAfterDrag`는 rollback 오류가 있어도 consumer `onAfterDrag`를 한 번 호출하고, rollback 오류를 우선 보존해 Core error boundary가 한 번 보고하도록 보정했다.
- `src/react/lifecycle.ts`에 private Area lifecycle primitive를 추출했다. component와 injected harness가 같은 registration/update/dispose/private-controller/ref/areaId lifecycle을 사용하며, initial registration 뒤 동일 option `updateArea()`는 생략한다.
- ReactDOMServer로 public Root/Area SSR output(provider-only, single area div/direct children)과 DOM global 없는 rendering을 추가 검증했다.
- RED: lifecycle module 부재 `ERR_MODULE_NOT_FOUND`를 먼저 확인했다. GREEN: React focused 10/10, `npm run verify` policy 26/26·unit 114/114, build/type/import, pre-commit, diff check 통과.

---

## Task 8 Review Fix Round 2

- `SortableArea` ref callback을 `useCallback`으로 안정화해 lifecycle instance가 동일한 controlled rerender에서 null/ref 재등록이 발생하지 않게 했다.
- Rootless Area는 private controller를 미리 만들지 않고 null Root controller와 factory를 lifecycle에 전달한다. lifecycle이 ref mount/unmount에 맞춰 owned controller를 생성·destroy한다.
- shared lifecycle rootless unmount/destroy regression을 추가했다.
- GREEN: React focused 11/11, `npm run verify` policy 26/26·unit 115/115, build/type/import, pre-commit, diff check 통과.

## Task 8 Review Fix Round 2 Core Coverage

- 실제 `createSortableScopeInternal`과 fake platform으로 source pointer activation을 수행해, same-group destination의 controlled items/direct-child mismatch(빈 DOM·placeholder-only)를 Core error boundary까지 검증했다. `INVALID_ELEMENT`은 onError에 한 번 도달하고 consumer before/change/after 및 setters는 모두 0회다.
- focused 12/12, `npm run verify` policy 26/26·unit 116/116, pre-commit, diff check 통과.

---

## Task 9 작업 일시

- 2026-08-12 (Asia/Seoul)

## Task 9 요약

- `comins-sortable/vue`에 generic `SortableRoot`와 controlled `SortableArea`만 공개했다.
- Root는 Vue provider만 렌더하고 `change` event를 한 번 emit하며, Area는 `modelValue`의 item slot 결과를 하나의 `data-comins-sortable-area` div direct child로 렌더한다.
- transfer는 source/destination `update:modelValue`를 change order대로 동기 emit한 뒤 Root `change`를 한 번 emit한다.
- private Vue lifecycle primitive는 최신 props/emit/options를 읽고, area ID·element 교체와 unmount에서 registration을 멱등 해제한다. Root 없는 Area는 ref mount 시 private one-area controller를 만들고 cleanup 시 destroy한다.
- Core activation 전 모든 registered binding의 placeholder 제외 direct child 수와 controlled item 수를 검증하며, cancelled/rejected 결과는 rollback 뒤 consumer callback을 호출한다. rollback/callback 오류가 겹치면 최초 오류를 Core error boundary로 전달한다.

## Task 9 변경 파일

- `src/vue.ts`, `src/vue/context.ts`, `src/vue/lifecycle.ts`, `src/vue/SortableRoot.ts`, `src/vue/SortableArea.ts`
- `test/unit/vue/adapter.test.ts`, `test/types/vue.test.ts`, `test/unit/helpers/framework-fixtures.ts`
- `package.json`, `test/package-foundation.node.mjs`
- `reports/2026-08-12.md`, `.superpowers/sdd/2026-08-07-comins-sortable-v1/task-9-report.md`

## Task 9 TDD 및 검증

- RED: Vue contract test는 `src/vue/context.js` 부재로 `ERR_MODULE_NOT_FOUND`, public type fixture는 `comins-sortable/vue` export 부재로 `TS2307`을 확인했다.
- GREEN: Vue focused suite에서 transfer emit order, controlled option update 비재등록, mount/unmount, rootless cleanup, 최신 props/emit, area ID 교체, rollback ordering/error, zero-child structural reject, injected Core error boundary, Vue SSR을 확인했다.
- `npm run typecheck`, `npm run build`, `npm run test:types`, DOM global 없는 `dist/vue.js` import와 `npm run verify`를 실행했다.

## Task 9 잔여 리스크

- Vue SSR의 multiple VNode/slot 배열은 fragment comment를 출력할 수 있으나 DOM wrapper element를 만들지 않으며, runtime structural guard는 Element child만 읽는다.
- injected harness와 fake platform으로 adapter/Core lifecycle을 검증했다. 실제 browser pointer E2E는 후속 browser task 범위다.
- 원격 push, PR, merge, publish, tag 및 Release는 수행하지 않았다.

---

## Task 9 Review Fix Round 1

- Vue public custom renderer에서 reactive props proxy가 같은 reference를 유지해 `areaId` 변경을 놓치던 문제를 수정했다. lifecycle은 현재 props proxy가 아니라 immutable registered options의 area ID와 비교하므로 `todo` unregister 뒤 `done` register가 보장된다.
- Vue `autoScroll` Boolean prop의 생략값을 `undefined`로 보존해 Core 기본값 `true`를 유지한다. 명시 `false`·`true` 전환은 각각 한 번의 `updateArea()`로 전달된다.
- Vue/React controller의 `onError` wrapper는 최신 consumer handler를 매 호출 시 읽는다. handler가 없으면 원래 오류를 throw해 Core가 platform report fallback을 수행하고, handler 오류는 Core가 별도로 report한다.
- RED: actual Vue custom renderer에서 reactive area ID가 `update:done`으로 잘못 전달되고, omitted autoScroll이 `false`가 되며, Vue/React 무-handler structural activation error가 platform reports에 남지 않는 것을 재현했다.
- GREEN: Vue/React focused 35/35, typecheck/build/type fixture, 두 built import, full verify, pre-commit 및 diff check를 통과했다.

---

## Task 10 작업 일시

- 2026-08-12 (Asia/Seoul)

## Task 10 요약

- `comins-sortable/svelte`에 branded generic `createSortableScope<T>()`와 direct `sortable<T>()` action만 공개했다. component, attachment, CustomEvent 및 Svelte runtime import는 추가하지 않았다.
- action은 supplied shared Scope 또는 private Scope를 사용하고, 최초 한 번 등록한 뒤 최신 items/itemKey/onItemsChange/options를 binding closure로 읽는다. area ID 또는 Scope 변경은 기존 registration을 멱등 해제한 뒤 안전하게 다시 등록한다.
- shared controlled transaction을 재사용해 transfer setter를 change order대로 실행한 뒤 enhanced root change를 한 번 호출한다. dropped는 finish, non-drop은 rollback 후 consumer callback을 실행하며 rollback 오류가 있어도 callback과 최초 오류 보존을 보장한다.
- Core activation 전 모든 binding의 placeholder 제외 direct-child count를 검증하며, `onError`가 없으면 원래 오류를 throw하여 Core platform reporter fallback을 유지한다. omitted `autoScroll`은 `undefined`로 그대로 전달한다.

## Task 10 변경 파일

- `src/svelte.ts`, `src/svelte/scope.ts`, `src/svelte/sortable.ts`
- `test/unit/svelte/action.test.ts`, `test/types/svelte.test.ts`, `test/unit/helpers/framework-fixtures.ts`
- `package.json`, `test/package-foundation.node.mjs`
- `reports/2026-08-12.md`, `.superpowers/sdd/2026-08-07-comins-sortable-v1/task-10-report.md`

## Task 10 TDD 및 검증

- RED: `src/svelte.js` 부재로 focused unit은 `ERR_MODULE_NOT_FOUND`, `comins-sortable/svelte` 부재로 type fixture는 `TS2307`을 확인했다.
- GREEN: action update/destroy, shared/private Scope, latest callback/item key, transfer setter/change order, area/Scope transition, rollback callback/error order, zero-child structural reject, Core platform fallback과 real Core after-drag error를 포함한 focused Svelte 10/10 통과.
- `npm run typecheck`, `npm run build`, `npm run test:types`, DOM global 없는 built Svelte import, built entry Svelte runtime import scan을 통과했다.
- `npm run verify`: policy 26/26, 전체 unit 149/149, build 및 public type fixture를 통과했다.
- `.githooks/pre-commit`과 `git diff --check`를 통과했다.

## Task 10 잔여 리스크

- sanitized lifecycle harness와 real Core fake platform으로 adapter 경계를 검증했다. 실제 Svelte compiler/browser pointer E2E는 Task 11 범위다.
- 원격 push, PR, merge, publish, tag 및 Release는 수행하지 않았다.

---

## Task 10 Review Fix Round 1

- area ID/Scope 전이를 transaction으로 보강했다. same-Scope 전이는 필요할 때만 기존 등록을 해제하고, candidate 등록이 실패하면 old options/Scope/ownership을 commit하지 않은 채 old registration을 복구한다. 복구도 실패하면 최초 transition 오류를 보존하고 owned resource를 정리한다.
- 새로운 private Scope candidate의 transition·초기 registration 실패 시 해당 Scope만 destroy하며, destroyed supplied Scope나 duplicate area ID 실패는 기존 action registration을 유지한다.
- action의 node/options/scope/factory/disposer를 nullable state object로 통합했다. destroy는 unregister→owned Scope destroy→참조 clear 순서이며 destroy 뒤 update는 no-op이다.
- Svelte controller가 active disposer를 추적하고 Scope destroy 시 Core/Binding registration을 identity-safe하게 전부 drain한다. action object가 남아도 shared Scope destroy 뒤 callback·registration은 남지 않는다.
- `itemKey` 누락 type rejection 및 type-only `Action` compatibility fixture를 추가했다. runtime build에는 Svelte import가 없다.
- focused Svelte 17/17, `npm run verify` policy 26/26·전체 unit 156/156, built import/runtime Svelte import scan, pre-commit 및 diff check를 통과했다.

## Task 10 Review Fix Round 2

- cross-Scope transition에서 candidate를 먼저 등록하면 두 Core Scope가 같은 node marker를 소유해 old disposer가 new marker를 제거하는 것을 actual Core/fake-platform RED로 재현했다.
- 새로운 순서는 private candidate 생성 후 old registration 해제, candidate registration, 성공 뒤 old owned Scope destroy다. candidate 실패 시 old registration과 marker를 복구하며 supplied Scope는 destroy하지 않고 private candidate만 정리한다.
- `todo→todo`, `todo→done`, candidate failure 뒤 valid retry, final destroy의 원래 attribute 정확 복구를 추가 검증했다.
- focused Svelte 21/21, `npm run verify` policy 26/26·전체 unit 160/160, built import/runtime Svelte import scan, pre-commit 및 diff check를 통과했다.

---

## Task 11 작업 일시

- 2026-08-12, 2026-08-14 (Asia/Seoul)

## Task 11 요약

- Vanilla, React StrictMode, Vue render function, Svelte 실제 `use:sortable` action을 사용하는 네 개의 최소 browser fixture와 Playwright 구성을 추가했다.
- interaction, geometry, rollback, auto-scroll, 50회 cancel/destroy resource lifecycle을 공통 adapter scenario로 검증한다. fixture 전역에는 area ID order, event reason, 마지막 operation, mount/destroy control만 노출하며 private engine, raw event, DOM snapshot 또는 consumer text는 노출하지 않는다.
- framework controller만 internal two-rAF commit verification factory를 사용하고 Vanilla public Scope/createSortable은 one-rAF를 유지한다. nested direct-child pointer target regression도 Core unit으로 고정했다.
- Svelte shared Scope destroy는 먼저 Core Scope를 종료하여 active drag rollback/after-drag가 binding transaction을 사용하는 동안 registration을 유지한 뒤 disposer를 drain한다.

## Task 11 브라우저 검증

- 최종 `npm run verify:e2e`: 205/205 통과, wall 20.5초, Playwright retry 0건.
  - Chromium non-resource: 67/67.
  - Firefox non-resource: 67/67.
  - Playwright WebKit non-resource: 67/67. 이는 WebKit engine evidence이며 Safari 인증이 아니다.
  - Chromium resource(serial): 4/4; 각 adapter가 50 cancel/destroy cycle 뒤 placeholder, dragging, over와 sanitized resource count 0을 확인했다.
- 최종 `npm run verify`: license, policy 27/27, typecheck, unit 162/162, build, public type fixtures 통과.
- 최종 `npm run verify:full`: 위 package gate와 205/205 E2E를 결합해 통과(E2E 20.7초).
- `npm run example:build`로 네 built fixture와 emitted relative runtime import를 확인했다. 로컬 server HTTP probe는 `/vanilla/` 200 HTML, encoded `..` path 404, POST 405 with `Allow: GET, HEAD`, HEAD CSS 200 with `text/css`를 확인했다.
- `.githooks/pre-commit`, `git diff --check`을 통과했다.

## Task 11 RED 및 안정화

- 최초 WebKit full run은 React empty-area geometry 60/61이었다. React effect가 fixture control을 준비하기 전에 호출되는 경쟁이 원인이었고, fixture readiness와 empty DOM 반영 대기를 추가한 뒤 해당 case 12회 반복 12/12 및 WebKit 61/61을 확인했다.
- 최초 all-project run은 Chromium Svelte empty-area geometry/interaction 2건으로 185/187이었다. mouse move 뒤 fixed 20ms 뒤 drop하는 helper가 pointer rAF destination calculation보다 앞서는 test race가 원인이었고, move 뒤 two-rAF boundary를 기다리도록 변경했다. Svelte empty geometry 30회 반복은 30/30, 독립 리뷰 전 all-project 두 번은 각각 187/187이었다.
- 독립 리뷰에서 geometry no-op, auto-scroll baseline false positive, consumer role/ARIA·reduced-motion·lifecycle ordering 누락, adapter control 불일치와 Svelte external CSS 누락을 확인했다. 실제 horizontal variable-size/gap order change, scroll delta, ARIA 보존, exact callback count, 공통 14-key control, injected Svelte CSS build gate로 보강했다.
- React·Vue `destroy` control이 Root가 아니라 Area만 제거해 `unmounted`를 보고하던 RED를 재현했다. 두 fixture가 실제 Root를 unmount하도록 수정하고, React Root는 layout cleanup 시 controller를 destroy하되 StrictMode 재설정에서는 새 controller를 지연 생성하는 private lifecycle로 보강했다.
- 리뷰 보정 중 React·Vue public Area에 테스트용 marker와 `role=list`가 주입되어 SSR 계약을 깨는 추가 RED를 확인했다. 테스트는 공개 `data-comins-sortable-area` marker를 사용하고 consumer role은 fixture wrapper가 소유하도록 수정했다.
- 공통 control이 `remount`로 정규화된 뒤 resource spec이 과거 `mount` 이름을 호출해 4건이 timeout되는 테스트 결함을 수정했다. focused resource 4/4와 최종 all-project 205/205 두 번을 확인했다.
- 이 작업의 final green run에는 test retry가 없었다. physical touch/pen device certification은 수행하지 않았다.

## Task 11 잔여 리스크 및 승인 대기

- `.github/workflows/verify.yml` browser job 추가는 승인 대기 범위라 변경하거나 commit하지 않았다. 준비된 제안 diff 범위는 `verify` 뒤의 `browser` job 1개이며 checkout/setup-node pinned actions, `npm ci --ignore-scripts`, Playwright Chromium/Firefox/WebKit install, `npm run verify:e2e`만 포함한다.
- 원격 push, PR, merge, publish, tag, Release 및 CI workflow edit은 수행하지 않았다.

---

## Playground 첫 수직 구현 작업 일시

- 2026-08-14 (Asia/Seoul)

## Playground 첫 수직 구현 요약

- `comins-table`/`comins-grid-layout` 형태의 독립 Vite consumer Playground를 추가했다. canonical route는 `/examples/:exampleId/:adapterId`, 기본 route는 `/examples/simple/react`이며 한국어/영어 locale을 로컬 key 하나로 관리한다.
- React shell은 예제 탐색, Vanilla/React/Vue/Svelte adapter 탭, 제어 패널, live workspace, controlled model JSON, event timeline, last operation, 실제 consumer source viewer를 제공한다.
- Simple, Two Lists, Handle, Empty Destination, Accept/Reject, Auto Scroll 6개 예제를 네 adapter의 실제 public API로 실행한다. framework 전환은 이전 runtime을 destroy한 뒤 mount하며 stale bridge publication을 차단한다.
- 기존 Task 11 fixture는 `.example-dist`와 port 4175에 유지하고, Playground는 `.playground-dist`와 port 4003 및 별도 `test/playground` suite로 격리했다.
- Vite 8.1.5는 필수 `lightningcss` MPL-2.0 license review 때문에 배제했다. license-compatible 보안 수정판 Vite 7.3.6을 사용하고 `nanoid`를 transitive 허용 범위 내 보안 수정판으로 갱신했다.

## Playground 변경 파일

- `example/index.html`, `example/fixtures-index.html`, `example/src/**`
- `vite.example.config.ts`, `playwright.playground.config.ts`
- `scripts/build-browser-fixtures.mjs`, `package.json`, `package-lock.json`, `tsconfig.json`, `.gitignore`
- `test/unit/playground/**`, `test/playground-build.node.mjs`, `test/playground/playground.spec.ts`
- `docs/superpowers/specs/2026-08-14-comins-sortable-playground-parity-design.md`
- `docs/superpowers/plans/2026-08-14-comins-sortable-playground-shell.md`

## Playground TDD 및 검증

- RED: navigation/locale, runtime host, demo data 모듈 부재를 각각 `ERR_MODULE_NOT_FOUND`로 확인했고, Playground build/browser config 부재를 각각 missing script/config 오류로 확인했다.
- Browser RED: handle target과 auto-scroll edge가 viewport 밖인 테스트 geometry 오류를 actual rect로 확인해 viewport-safe interaction으로 보정했다. 병렬 React/Vue에서는 module mount가 registration effect보다 먼저 `Live`를 표시할 수 있어 두 animation frame 뒤 ready를 반환하도록 보정했다.
- 빈 대상 예제에서 목록에 아이템이 하나만 남으면 Grid의 기본 `align-content: normal`이 64px 카드를 88px 최소 목록 높이까지 늘리는 RED를 확인했다. 목록 Grid를 `align-content: start`로 고정하고 네 adapter의 실제 두 번 이동 후 카드 높이 회귀를 추가했다.
- 전체 검증에서 Svelte fixture test와 Playground build test가 동일 `.example-dist`를 동시에 재생성하는 기존 race가 두 번 재현됐다. 정책 테스트를 직렬 실행하도록 제한해 테스트 내용과 수를 유지한 채 출력 디렉터리 경합을 제거했다.
- `npm run verify`: license, policy 28/28, typecheck, unit 169/169, package build, public type fixtures 통과.
- `npm run verify:e2e`: 기존 Task 11 205/205 통과. Playground spec을 별도 test directory로 분리해 기존 5개 fixture spec만 수집됨을 확인했다.
- `npm run verify:playground`: 45/45 통과. Chromium 15/15, Firefox 15/15, Playwright WebKit 15/15이며 retry는 0이다.
- Chromium four-adapter transfer 반복: 40/40 통과.
- `npm audit`: 취약점 0건.

## Playground 잔여 범위

- Vue.Draggable parity 17개 중 첫 6개를 제공한다. Clone/Custom Clone/Clone on Control, custom host/table/third-party, transition/animation, header/footer, nested/controlled nested 11개는 승인된 parity 설계의 후속 수직 구현 범위다.
- Playwright WebKit은 engine evidence이며 실제 Safari 인증이 아니다. physical touch/pen 검증도 아직 수행하지 않았다.
- CI browser job, 원격 push, PR, merge, publish, tag 및 Release는 수행하지 않았다.
