# 2026-08-07 Private Package Boundary

## 작업 일시

- 2026-08-07 (Asia/Seoul)

## 요약

- 승인된 Comins Sortable v1 설계의 Task 1을 구현했다.
- `comins-sortable@0.0.0-development` private ESM package boundary와 npm
  lockfile을 추가했다.
- runtime dependency는 두지 않고 React, React DOM, Vue, Svelte를 optional
  peer로 고정했다.
- Core public type과 값 비노출 `SortableError` 계약을 `./core` export로
  추가했다.
- package/lock root, peer 범위, lockfile SPDX, copied/generated material,
  asset surface를 검사하는 package-aware license gate를 추가했다.
- 원격 publish, tag, Release, package artifact 생성은 수행하지 않았다.

## 변경 파일

- `package.json`, `package-lock.json`, `package-boundary.mjs`
- `tsconfig.json`, `tsconfig.build.json`
- `scripts/build-package.mjs`, `scripts/run-unit-tests.mjs`
- `src/core.ts`, `src/core/model.ts`, `src/core/errors.ts`
- `LICENSE_SCOPE.json`, `scripts/check-licenses.mjs`
- `test/package-foundation.node.mjs`, `test/license-gates.node.mjs`
- `test/sensitive-data-gates.node.mjs`
- `test/unit/core/errors.test.ts`
- `test/types/core.test.ts`, `test/types/tsconfig.json`
- `.github/workflows/verify.yml`, `AGENTS.md`, `README.md`
- `reports/2026-08-07.md`

## TDD 증거

- Package/policy RED:
  `node --test test/package-foundation.node.mjs test/license-gates.node.mjs test/sensitive-data-gates.node.mjs`
  - 25개 중 17개 통과, 8개 실패.
  - package manifest, lockfile, package boundary, package-aware license 분류,
    CI와 Module Guidance 부재를 검출했다.
- Core RED:
  - `npm run test:run`: `src/core.js` 부재로 실패.
  - `npm run test:types`: Core public module 부재로 실패.
- Core GREEN:
  - `npm run test:run`: 1/1 통과.
  - `npm run test:types`: 통과.
- License order-independence RED:
  `node --test test/license-gates.node.mjs`
  - 16개 중 15개 통과, JSON peer key 순서 변경 1개 실패.
- License order-independence GREEN:
  - license gate 16/16 통과.
- 최종 package/policy GREEN:
  - policy test 26/26 통과.

## 검증

- `npm ci --ignore-scripts`: 58 packages 설치, lifecycle script 미실행.
- `npm run verify`: 통과.
  - license gate 통과, 출력 없음.
  - policy test 26/26 통과.
  - TypeScript typecheck 통과.
  - Core unit test 1/1 통과.
  - ES2020 ESM build 통과.
  - public type fixture 통과.
- `npm audit --audit-level=high`: 취약점 0건.
- lockfile: version 3, 103 package entries, routine SPDX만 확인.
- `dist/core.js`, `dist/core.d.ts`, `dist/core.js.map`: 생성 확인.
- staged Gitleaks 8.30.1 hook: 통과.
- public Git identity range gate: 통과.
- `git diff --check`: 통과.

## 잔여 리스크

- Task 2 이후의 immutable operations, DOM engine, Placeholder, Vanilla,
  React, Vue, Svelte adapter는 아직 구현하지 않았다.
- 현재 package export는 Task 1 범위인 `./core`만 제공한다. 최종 root,
  framework, CSS export는 해당 구현 Task에서 단계적으로 추가한다.
- Browser E2E, exact artifact, consumer smoke, public release gate는 아직
  적용 단계가 아니다.
- `.codex/config.toml`은 repository trust가 있는 실행에서만 적용된다.
- 원격 push, PR, merge, publish는 수행하지 않았다.
