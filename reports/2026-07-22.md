# 2026-07-22 Local Integration And Residual-Risk Audit

## 작업 일시

- 2026-07-22 00:38 KST

## 요약

- `codex/privacy-baseline-simple-sortable`을 기존 로컬 계획 커밋을 보존하며 `main`에 병합했다.
- package boundary를 만들지 않고 Contract v1.2, Gitleaks, raw Git identity, local hook, security CI 계약만 통합했다.
- 저장소 로컬 Git 설정에 `core.hooksPath=.githooks`를 적용했다.

## 검증

- `node --test test/*.node.mjs`: 5/5 통과
- `origin/main..main` raw identity 검사: 통과
- `origin/main..main` Gitleaks 검사: 통과
- `git diff --check origin/main..main`: 통과
- tracked pre-commit 훅 실행: 통과
- `package.json`과 publish workflow 부재 확인: 통과
- worktree 상태: clean

## 원격 감사

- Private Vulnerability Reporting, Secret Protection, Push Protection, Dependabot alerts는 활성 상태다.
- `main` ruleset과 classic branch protection은 구성되지 않았다.
- 새 보안 workflow는 로컬 `main`에만 있으므로 원격 required check로 아직 사용할 수 없다.

## 잔여 조치

- 원격 `main` 반영 후 보안 workflow를 실행하고 Sortable `main` ruleset을 생성해야 한다.
- package boundary 승인 전에는 package artifact와 publish gate를 추가하지 않는다.
- 원격 push와 GitHub 설정 변경은 이번 로컬 병합에서 수행하지 않았다.

## GitHub Service Committer Compatibility — 01:32 KST

### 요약

- Dependabot 커밋의 GitHub service committer가 공통 public-identity gate에서 차단되는 문제를 재현하고 수정했다.
- package boundary를 추가하지 않고 현재 Git/CI identity gate만 교정했다.

### 변경 파일

- `scripts/check-public-identities.mjs`
- `test/sensitive-data-gates.node.mjs`

### 검증

- 회귀 테스트 수정 전 실패 및 수정 후 통과 확인
- `node --test test/sensitive-data-gates.node.mjs`: 6/6 통과
- `node --test test/*.node.mjs`: 6/6 통과

### 잔여 리스크

- checker 동작 범위의 잔여 리스크는 확인되지 않았다. package boundary 승인 전에는 artifact/publish gate를 추가하지 않는다.
