# 2026-07-21 Sensitive Data Baseline

## 작업 일시

- 2026-07-21 (Asia/Seoul)

## 요약

- Comins Contract v1.2의 공통 민감정보 규칙을 채택했다.
- Gitleaks 8.30.1, raw Git identity 검사, fail-closed pre-commit/pre-push 훅, credential-free CI를 추가했다.
- 아직 package boundary가 없으므로 `package.json`과 publish workflow는 추가하지 않았다.
- 첫 공개 릴리스 전에는 하나의 `npm pack --json --ignore-scripts` 산출물을 검증·스캔·게시해야 한다는 계약만 문서화했다.

## 변경 파일

- `AGENTS.md`
- `SECURITY.md`
- `.gitignore`
- `.gitleaks.toml`
- `.githooks/pre-commit`
- `.githooks/pre-push`
- `.github/workflows/verify.yml`
- `scripts/check-public-identities.mjs`
- `test/sensitive-data-gates.node.mjs`

## 검증

- `node --test test/sensitive-data-gates.node.mjs`: 5/5 통과
- `sh -n .githooks/pre-commit && sh -n .githooks/pre-push`: 통과
- Ruby YAML parser로 `.github/workflows/verify.yml` 로드: 통과
- `git diff --check`: 통과
- Governance에서 확정한 공통 Gitleaks 설정, identity checker, pre-commit, pre-push 파일과 바이트 비교: 통과

## 잔여 이슈

- 원격 CI는 아직 실행하지 않았다.
- package boundary 승인 전까지 publish workflow와 package artifact gate는 의도적으로 미구현 상태다.
- 원격 push, npm publish, GitHub 설정 변경은 수행하지 않았다.
