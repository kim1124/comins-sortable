# 2026-08-28 Playground 정리 및 0.1.0 공개 후보

## 작업 일시

- 2026-08-28 (Asia/Seoul)

## 요약

- Playground 좌측 메뉴의 API 보조 제목과 parity 진행 카드를 제거했다.
- 실행 화면 우측의 제어 모델 및 마지막 작업 패널을 제거하고 작업 영역을
  전체 너비로 확장했다.
- 공통 Comins 임시 브랜드 마크로 사각형 `co` 텍스트 로고를 적용했다.
- `comins-sortable@0.1.0` 최초 공개 후보로 package, README, CHANGELOG,
  SECURITY 및 Module Guidance를 정리했다.
- 정확한 package artifact, consumer smoke, Gitleaks, npm maintainer identity,
  OIDC staged publishing을 소유하는 module release workflow를 추가했다.

## 변경 파일

- Playground: `example/src/app/PlaygroundApp.tsx`,
  `example/src/app/messages.ts`, `example/src/styles.css`
- Package: `package.json`, `package-lock.json`, `README.md`, `CHANGELOG.md`,
  `SECURITY.md`, `AGENTS.md`
- Release: `.github/workflows/publish.yml`,
  `scripts/verify-package-artifact.mjs`, `scripts/consumer-smoke.mjs`,
  `scripts/check-npm-maintainer.mjs`, `scripts/check-licenses.mjs`
- Tests: `test/playground/playground.spec.ts`, `test/release-gates.node.mjs`,
  package, license, sensitive-data policy tests

## 수행한 검증

- `npm run verify`: 통과
  - policy 32/32, unit 193/193, license, typecheck, build, public type fixture
- `npm run verify:e2e`: Chromium, Firefox, Playwright WebKit 및 resource
  `205/205` 통과
- `npm run verify:playground`: production build 및 3-engine `93/93` 통과
- Playwright CLI 실제 화면 확인: `co` 마크, 단순화된 메뉴, 전체 너비 실행 화면
- `npm pack --dry-run --json --ignore-scripts`: 0.1.0 allow-list 113개 파일 확인
- Gitleaks 8.30.1 및 repository hooks 경로 확인
- `git diff --check`: 통과

정책 테스트 최초 실행은 공개 manifest 전환 중 남은 삭제 변수 참조로 5건
실패했다. 원인을 단일 참조로 분리해 수정한 뒤 policy 32/32와 전체 package
gate를 재실행해 통과했다. `npm pack --dry-run` 최초 실행의 사용자 cache
`EPERM`은 sandbox 경계였으며 작업 전용 임시 cache로 동일 명령이 통과했다.

## 잔여 이슈

- 실제 release tarball은 보호된 source merge 이후 단 한 번 생성하고, 추출
  Gitleaks 및 exact consumer smoke를 통과한 동일 artifact만 bootstrap publish한다.
- 최초 npm 공개는 maintainer web login 및 2FA가 필요하다. 공개 직후 exact
  `publish.yml`과 `npm` environment를 stage-only trusted publisher로 등록해야 한다.
- Playwright WebKit 결과는 실제 Safari 인증이 아니다. Physical touch 및 pen도
  이번 release 인증 범위에 포함하지 않는다.
- 기존 `favicon.ico` 404는 이번 UI 및 release 범위 밖으로 유지한다.

## 공개 후 사고 및 로컬 보완

- `comins-sortable@0.1.0` 공개 메타데이터에서 npm 계정의 비서비스 이메일이
  확인되어 즉시 unpublish했다. 정확한 버전과 tarball은 현재 404이며,
  레지스트리에는 이메일 필드 없는 unpublish tombstone만 남아 있다.
- `0.1.0`은 재사용할 수 없어 다음 공개 후보를 `0.1.1`로 변경했다.
- 기존 npm maintainer 검사가 이름만 비교하고 이메일을 버리던 원인을
  수정했다. 교체 검사는 provider-managed name/email과 authenticated profile,
  current registry maintainer, exact published metadata를 각각 fail-closed로
  검증하며 실제 identity 값을 출력하지 않는다.
- release workflow에 전체 reachable Git history identity 검사를 추가했다.
  현재 legacy main 이력은 이 검사에 실패하므로 이력 정리가 별도 승인되어
  완료되기 전에는 release가 차단된다.
- npm 계정 이메일 변경, repository/environment secrets 생성, trusted publisher
  등록, Git 이력 재작성, 재배포는 이번 로컬 코드 변경 범위에 포함하지 않았다.
